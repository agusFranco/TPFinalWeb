function mostrarModal() {
    $("#successModal").modal({
        fadeDuration: 1000,
        fadeDelay: 0.50,
        escapeClose: false,
        clickClose: false        
    });
}

function initializeDetalle() {
}

$(document).ready(function () {
    initializeDetalle();
});